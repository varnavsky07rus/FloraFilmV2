<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <!-- Горизонтальная линия, идущая от вертикальной -->
    <View
        android:layout_width="25dp"
        android:layout_height="2dp"
        android:layout_marginStart="18dp"
        android:layout_marginTop="35dp"
        android:background="?attr/IconsTintThemeInverse" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <LinearLayout
            android:id="@+id/linearLayoutTitleClick2"
            android:layout_width="match_parent"
            android:layout_height="70dp"
            android:clickable="true"
            android:foreground="?attr/selectableItemBackground"
            android:orientation="horizontal">

            <ImageView
                android:id="@+id/imageViewFiles"
                android:layout_width="70dp"
                android:layout_height="70dp"
                android:layout_gravity="center"
                android:layout_marginStart="8dp"
                android:padding="16dp"
                android:tint="?attr/IconsTintThemeInverse"
                app:srcCompat="@drawable/rounded_play_circle_24" />

            <TextView
                android:id="@+id/textViewTitleFiles"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_gravity="center"
                android:layout_marginStart="16dp"
                android:layout_marginEnd="8dp"
                android:gravity="center_vertical"
                android:lines="2"
                android:maxLines="2"
                android:textSize="20sp"
                tools:text="MP4 1080p" />
        </LinearLayout>

        <LinearLayout
            android:id="@+id/linearLayoutFaF"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical" />
    </LinearLayout>
</RelativeLayout>